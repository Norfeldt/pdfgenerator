\documentclass{article}
\usepackage{fancyhdr}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{lastpage}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{svg} % To handle SVG images
\usepackage{ifthen} % Add this line to use ifthen

% Page geometry
\geometry{a4paper, margin=1in}

% Header and Footer settings
\pagestyle{fancy}
\fancyhf{} % Clear header and footer

% Define a command to include the header image, considering both PNG and SVG formats
$if(header-image)$
\ifthenelse{\equal{$header-image$}{logo.svg}}{%
    \newcommand{\headerimage}{\includesvg[height=1cm]{$header-image$}} % Use includesvg for SVG files
}{%
    \newcommand{\headerimage}{\includegraphics[height=1cm]{$header-image$}} % Use includegraphics for other formats
}
$else$
\newcommand{\headerimage}{}
$endif$

% Header
\fancyhead[L]{\textbf{Your Company Name}} % Customize this text
\fancyhead[R]{\headerimage} % Use the image provided in the markdown metadata

% Footer
\renewcommand{\footrulewidth}{0.4pt} % Line at the top of the footer
\fancyfoot[L]{\scriptsize Company Name \\ Address Line 1 \\ Address Line 2}
\fancyfoot[C]{\scriptsize Version 1.0, Document ID}
\fancyfoot[R]{\scriptsize Page \thepage\ of \pageref{LastPage}}

\setlength{\headheight}{32.05278pt}
\addtolength{\topmargin}{-20.05278pt}

% Begin document
\begin{document}

% Title Page
\begin{titlepage}
    \centering
    \vspace*{2cm}
    {\Huge\bfseries The Quantum Resonance Paradigm\par}
    \vspace{1cm}
    {\Large Author Name\par}
    \vfill
    {\large \today\par}
\end{titlepage}

% Table of Contents
\tableofcontents
\newpage

% Include Markdown content here
%\input{content.tex}


% In order to include the content list from markdown file to the latex, it should match with the name of the markdown file
$body$

\end{document}

